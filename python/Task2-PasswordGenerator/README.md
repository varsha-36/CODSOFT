# CODSOFT – Python Internship  
## Task 2: Password Generator (CLI)

Generates strong passwords using **cryptographically secure** randomness (`secrets`).

### Features
- Choose **length**
- Toggle **lowercase / uppercase / digits / symbols**
- Option to **exclude similar** (`I l 1 O 0`) and **ambiguous** (`{ } [ ] / \ ...`)
- Simple interactive CLI

### How to Run
```bash
python main.py
